---
title: Checkbox Element
description: Learn how to use the Checkbox element in JUX.

tableOfContents:
  minHeadingLevel: 3
  maxHeadingLevel: 6
---
import { Image } from 'astro:assets';
import CaptionImage from "../../../../components/CaptionImage.astro";

import checkboxElement from '../../../../assets/editor/elements/checkbox.png'
import checkboxAnatomy from '../../../../assets/editor/elements/checkbox_anatomy.png'
import checkboxMatrix from '../../../../assets/editor/elements/checkbox_matrix.png'

<CaptionImage caption="Checkbox Element">
  <Image src={checkboxElement} alt="Checkbox Element" />
</CaptionImage>

### Technical Definition
The Checkbox Element in Jux represents an interactive control that allows users to select or deselect a single value. It's typically used in forms and can be in a checked, unchecked, or indeterminate state.

### Anatony and Styling

The checkbox element consists of two main parts:

- A box with assets for 'checked', 'unchecked', and 'indeterminate' states
- A text wrapper containing a label and helper text (which can be removed if not needed)

You can:

- Style each part independently based on the checkbox's state and properties using the parent context module
- Hide specific elements in certain states
- Modify SVG assets for check and indeterminate icons via the 'asset content' property

<CaptionImage caption="Checkbox Anatomy">
  <Image src={checkboxAnatomy} alt="Checkbox Anatomy" />
</CaptionImage>

### Props and States

Checkbox properties include:

1. Interactive state:
    - Default
    - Hover
    - Active

2. Checked state (special):
    - checked
    - unchecked
    - indeterminate

Checkbox properties can be extended based on your design needs, allowing for custom properties to enhance functionality and design variations. However, core properties like 'disabled' can't be removed to ensure proper web behavior.

### Matrix

<CaptionImage caption="Checkbox Matrix">
  <Image src={checkboxMatrix} alt="Checkbox Matrix" />
</CaptionImage>

### Special Mechanics

The checkbox includes two logical slots:

- Checkbox primitive: Contains the visual 'box' of the element. This slot must not be empty for proper functionality.
- Trigger (hit area): Allows objects inside to change the checkbox state. Can be empty or contain other objects.