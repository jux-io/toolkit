---
title: Tokens
description: Learn how to create and manage design tokens in JUX.

tableOfContents:
  minHeadingLevel: 2
  maxHeadingLevel: 6
---
import { Image } from 'astro:assets';
import nonAlias from '../../../../assets/editor/tokens/alias_non_alias.png'
import simpleVsComposite from '../../../../assets/editor/tokens/simple_vs_composite.png'
import tokensPage from '../../../../assets/editor/tokens/tokens_page.png'
import ddpTokens from '../../../../assets/editor/tokens/ddp_tokens.png'
import tokenManagment from '../../../../assets/editor/tokens/token-management.png'

## What are Tokens ?

Design tokens are reusable visual elements that represent design choices in user interface design. Tokens provide a unified language for communicating design decisions, allowing for a multi-layered architecture of styles which are then used in code.

Design tokens are defined and stored in a central location (in Jux - the token management page), allowing designers to quickly and easily apply consistent styles across various products and projects. Tokens include styling information such as colors, typography, dimensions (for size, space etc) etc. This allows for a more efficient and scalable design process, as designers can reuse and build upon existing tokens instead of starting from scratch each time.

With the use of the JSON format, design tokens allow developers to implement design decisions across code bases, while being agnostic to the framework. Using design tokens in this manner allows teams to collaborate more easily, align their designs with brand design guidelines, and increase efficiency.

It’s important to keep in mind that there are two properties that tokens can have, with two options for each property: *alias vs non-alias* tokens:

<Image src={nonAlias} alt="Alias vs. Non-Alias Tokens" />

and simple vs composite tokens:

<Image src={simpleVsComposite} alt={"Simple vs. Composite Tokens"} />

For more references check out this [article](https://blog.jux.io/cp/135867686) for a deeper dive. Also, Tokens naming is an important topic recommended to read about [here](https://medium.com/eightshapes-llc/naming-tokens-in-design-systems-9e86c7444676).

### Jux token management 

Jux tokens are managed in a dedicated page, accessed through the left sidebar navigation panel.

<Image src={tokenManagment} alt={"Token management"} />




### Alias vs. Non-Alias Tokens

<Image src={nonAlias} alt="Alias vs. Non-Alias Tokens" />

### Simple vs. Composite Tokens

<Image src={simpleVsComposite} alt={"Simple vs. Composite Tokens"} />

:::note
For a deeper understanding of these concepts, we recommend reading [this article on token properties](https://blog.jux.io/cp/135867686) and [this article](https://medium.com/eightshapes-llc/naming-tokens-in-design-systems-9e86c7444676) on token naming.
:::

## Jux Token Management

Jux provides a dedicated token management interface, accessible through the left sidebar navigation panel.

<Image src={tokensPage} alt={"Jux tokens page"} />

Jux currently supports the following token types (see our roadmap for future token support):

- [Color](/editor/tokens/color)
- [Typography](/editor/tokens/typography)
- [Font family](/editor/tokens/font-family)
- [Dimension](/editor/tokens/dimension)

### Creating Token Sets

Jux allows you to create and manage multiple token sets, each serving a specific purpose in your design system:

- Core set:
  - Contains fundamental tokens (e.g., brand colors, primary typography styles)
  - Serves as a shared resource across all themes
  - Enables easy reusability of common design decisions
  - Promotes consistency across your entire design system


- Theme sets:
  - Comprise alias tokens that reference core tokens (or a fixed token value)
  - Allow for different UI modes (e.g., light/dark, dense/loose)
  - All theme sets have identical structure, mirroring each other
  - When you add or remove a token in one theme set, the change is automatically reflected across all other theme sets
  - While structurally identical, each theme set can point to different core tokens, enabling varied UI modes

### Using Tokens in Jux Editor

Tokens created in the management page are available for selection in the Editor's [Dynamic Design Panel (DDP)](/editor/dynamic-design-panel) inputs.

Tokens are easier to create while testing design decisions (values) on different components on the canvas itself. To create tokens in the editor view on canvas, hover over the DDP fields to reveal the token icon menu button and click it. The "Create token" action is placed at the bottom of the dropdown. New tokens will appear in the management page as well.

<Image src={ddpTokens} alt={"Dynamic Design Panel Tokens"} />

### Sharing Design Tokens with Developers

[Jux CLI](/developers/jux-editor) provides a straightforward method for developers to access the latest design assets. This process allows immediate integration of the latest design tokens and components into the codebase.

Maintaining an organized, shared token library is crucial for effective team collaboration. It ensures that both you and developers are working with the most up-to-date design system elements.

We're here to assist you in establishing your team's token library or to receive your feedback on our token management system. Your input helps us continually improve the Jux experience for both you and developers.